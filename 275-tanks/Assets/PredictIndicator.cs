using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PredictIndicator : MonoBehaviour
{
    [SerializeField] float predict;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        //transform.position = Player.PredictedPosition(predict);
    }
}
